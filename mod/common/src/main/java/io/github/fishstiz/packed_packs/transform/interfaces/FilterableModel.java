package io.github.fishstiz.packed_packs.transform.interfaces;

public interface FilterableModel {
    default void packed_packs$filterHidden(boolean filter) {
    }

    default void packed_packs$reset() {
    }
}
